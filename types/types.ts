export type ProjectType = {
    title: string;
    description: string;
    year: string;
    stack: string[];
    link: string;
    image: string;
}

export type SocialType = {
    value: string;
    icon: string;
}